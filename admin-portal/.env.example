# Copy to .env for local development.
# The deployed Admin Portal (its own Vercel project) sets this same
# variable directly in Vercel's project environment variables instead.
VITE_API_BASE_URL=https://ehs-next.vercel.app/api/v1
